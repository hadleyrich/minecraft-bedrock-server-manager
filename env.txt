# Server Configuration
PORT=3001
#linux:
DATA_DIR=/root/mcbedrock-server

#windows:
#DATA_DIR=C:\MINECRAFTSERVER\data

# Docker Configuration
# Docker image to use (optional, defaults to itzg/minecraft-bedrock-server)
#BEDROCK_IMAGE=itzg/minecraft-bedrock-server

# Enable SSH
ENABLE_SSH: TRUE

# CORS Configuration (optional)
# Comma-separated list of allowed origins for CORS
# Default: http://localhost:3001,http://127.0.0.1:3001
# Use * to allow all origins (not recommended for production)
#ALLOWED_ORIGINS=http://localhost:3001,http://192.168.1.100:3001
#
#
# Optional: Specify the Docker network for containers to use
# If not set, containers will use Docker's default bridge network with port mapping
#
# Network Types:
# - bridge (default): Standard Docker bridge with host port mapping (19132, 19133, etc.)
# - macvlan/ipvlan: Containers get their own IP addresses on your LAN
#   - Each container uses port 19132 internally (standard Minecraft port)
#   - Appears as LAN game automatically
#   - No port mapping needed
#
# Example macvlan network:
# docker network create -d macvlan --subnet=192.168.1.0/24 --gateway=192.168.1.1 -o parent=eth0 minecraft-macvlan
#DOCKER_NETWORK=minecraft-macvlan
#DOCKER_NETWORK=minecraft-network

# Authentication
LOGIN_PASSWORD=minecraft123
MAX_LOGIN_ATTEMPTS=5
LOGIN_LOCKOUT_MINUTES=20
